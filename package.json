{
  "name": "project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index",  // npm run start запускает входной файл приложения
    "dev": "nodemon index"  
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",           // обработчик запросов
    "express-handlebars": "^5.3.1", // HTML движок
    "mongoose": "^5.9.12"   // отвечает за работу с базой данных
  },
  "devDependencies": {
    "nodemon": "^2.0.3"  // автоматическая перезагрузка сервера
  }
}
